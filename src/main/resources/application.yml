spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tracker_database?serverTimezone=UTC
    username: qwqruserty
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 8080